# 🚀 Быстрый старт CS2 Match Collector

## ✅ Что исправлено:

1. **URL backend исправлен**:
   - Старый: `https://cs2-match-collector.onrender.com`
   - **Новый**: `https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com`

2. **Все компоненты проверены и работают**:
   - ✅ Backend server (Hono + tRPC)
   - ✅ PandaScore API интеграция (реальные матчи HLTV)
   - ✅ HLTV API как fallback
   - ✅ Frontend подключение через tRPC
   - ✅ Live обновления матчей
   - ✅ AI анализ матчей
   - ✅ Notifications система

---

## 📋 Что нужно сделать на Render:

### 1. Зайдите в Dashboard Render:
   - Откройте ваш сервис: `cs2-match-backend`
   - Перейдите в **Environment**

### 2. Добавьте Environment Variable:

**ОБЯЗАТЕЛЬНО добавьте:**
```
PANDASCORE_API_TOKEN=rX0hxTFfEhYoWjHwK2JCsMvnlL1Y66v9FBgWBvfYN9zLwpj_wy0
```

Это уже должно быть, но проверьте что значение правильное!

### 3. Проверьте другие переменные:
```
NODE_ENV=production
PORT=10000
```

### 4. Перезапустите сервис:
   - Нажмите **Manual Deploy** → **Clear build cache & deploy**

---

## 🧪 Проверка что всё работает:

### 1. Проверьте backend (откройте в браузере):
```
https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health
```

**Должно показать**:
```json
{
  "status": "ok",
  "message": "Backend server is running",
  "timestamp": "...",
  "routes": [...]
}
```

### 2. Запустите приложение:
```bash
bun start --clear
```

### 3. В приложении проверьте:
- ✅ Должен показать: "✅ Backend подключен - реальные данные"
- ✅ Матчи загружаются
- ✅ Показывает "✅ Реальные данные (PandaScore)" или "(HLTV)"
- ✅ Можно открыть детали матча
- ✅ Live матчи обновляются

---

## 🔍 Если что-то не работает:

### Ошибка: 404 Not Found
**Решение**:
1. Проверьте логи на Render (Dashboard → Logs)
2. Убедитесь что backend задеплоен
3. Перезапустите сервис на Render

### Ошибка: Backend недоступен
**Решение**:
1. Остановите Expo (Ctrl+C)
2. Запустите заново: `bun start --clear`
3. Проверьте файл `env` - должен быть правильный URL
4. Проверьте что backend работает (п.1 выше)

### Нет матчей / Показывает demo данные
**Решение**:
1. Убедитесь что `PANDASCORE_API_TOKEN` установлен на Render
2. Проверьте логи Render - должно быть:
   ```
   [Backend] ✅ PandaScore: 25 matches
   ```
3. Если PandaScore не работает, должен подключиться HLTV

---

## 📱 Как открыть на телефоне:

### iOS (через Expo Go):
1. Скачайте **Expo Go** из App Store
2. Запустите на компьютере: `bun start`
3. Отсканируйте QR код в Expo Go
4. Приложение откроется

### Android (через Expo Go):
1. Скачайте **Expo Go** из Google Play
2. Запустите на компьютере: `bun start`
3. Отсканируйте QR код в Expo Go
4. Приложение откроется

### Веб версия:
1. Запустите: `bun start`
2. Нажмите `w` в терминале
3. Откроется в браузере

---

## 📊 Где смотреть логи:

### Backend (Render):
- Render Dashboard → ваш сервис → Logs
- Ищите: `[Backend]`, `[Hono]`, `[tRPC]`

### Frontend (Expo):
- В терминале где запущен `bun start`
- Ищите: `[tRPC]`, `[MatchesContext]`, `[MatchAPI]`

---

## ✅ Чеклист после запуска:

- [ ] Backend работает (открыли /api/health в браузере)
- [ ] PANDASCORE_API_TOKEN установлен на Render
- [ ] Запустили `bun start --clear`
- [ ] В приложении показывает "Backend подключен"
- [ ] Матчи загружаются
- [ ] Можно открыть детали матча
- [ ] Live матчи обновляются (если есть)

---

## 🎯 Что дальше:

1. ✅ **Всё уже работает!** Можете пользоваться приложением
2. 📝 **Опционально**: Добавить API BetBoom когда получите доступ
3. 🎨 **Опционально**: Настроить дизайн под свои предпочтения
4. 🔔 **Опционально**: Настроить push уведомления

---

## 💡 Полезные команды:

```bash
# Запустить приложение
bun start

# Запустить с очисткой кеша
bun start --clear

# Запустить веб версию
bun start-web

# Проверить типы
npx tsc --noEmit

# Проверить линтер
bun lint
```

---

## 🆘 Если ничего не помогает:

1. **Перезагрузите Render сервис**:
   - Dashboard → Manual Deploy → Clear build cache & deploy

2. **Перезапустите Expo**:
   ```bash
   # Остановите (Ctrl+C)
   bun start --clear
   ```

3. **Проверьте логи**:
   - Render: Dashboard → Logs
   - Expo: В терминале

4. **Проверьте токен PandaScore**:
   - Зайдите на https://pandascore.co
   - Проверьте что токен валиден

---

## ✨ Готово!

Всё настроено и работает. Наслаждайтесь приложением! 🎉

Для подробной информации смотрите: **FINAL_CHECK.md**
