# ✅ Render Dashboard - Что проверить

## 🎯 Ваш сервис на Render

**URL**: https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com

---

## 1️⃣ Environment Variables (САМОЕ ВАЖНОЕ!)

### Перейдите: Dashboard → cs2-match-backend → Environment

### Убедитесь что есть эти переменные:

#### ✅ NODE_ENV
```
NODE_ENV=production
```

#### ✅ PORT  
```
PORT=10000
```

#### ⚠️ PANDASCORE_API_TOKEN (ОБЯЗАТЕЛЬНО!)
```
PANDASCORE_API_TOKEN=rX0hxTFfEhYoWjHwK2JCsMvnlL1Y66v9FBgWBvfYN9zLwpj_wy0
```

**ВАЖНО**: Это ключ для получения реальных данных о матчах с PandaScore!

---

## 2️⃣ Build & Start Commands

### Перейдите: Dashboard → cs2-match-backend → Settings

### Build Command должен быть:
```bash
curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install
```

### Start Command должен быть:
```bash
export PATH="$HOME/.bun/bin:$PATH" && bun run backend/server.ts
```

---

## 3️⃣ Проверка работоспособности

### A. Через браузер:
Откройте в браузере:
```
https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health
```

**Должно показать**:
```json
{
  "status": "ok",
  "message": "Backend server is running",
  "timestamp": "2025-01-07T12:34:56.789Z",
  "routes": [
    "/api/trpc/example.hi",
    "/api/trpc/matches.all"
  ]
}
```

### B. Через curl (в терминале):
```bash
curl https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health
```

---

## 4️⃣ Логи (где смотреть что происходит)

### Перейдите: Dashboard → cs2-match-backend → Logs

### Что должно быть в логах:

#### ✅ Успешный запуск:
```
[Server] 🚀 Starting server on port 10000
[Server] ✅ Server is running on http://localhost:10000
[Server] 📡 Health check: http://localhost:10000/api/health
[Server] 🔌 tRPC endpoint: http://localhost:10000/api/trpc
```

#### ✅ Когда приходит запрос на матчи:
```
[Hono] 📥 POST /api/trpc/matches.all
[Backend] 🚀 START: Fetching matches from multiple sources...
[Backend] 🔍 Trying PandaScore API...
[Backend] ✅ PandaScore: 25 matches
[Backend] 🔍 Trying HLTV API...
[Backend] ✅ HLTV: 15 matches
[Backend] ✅ Total unique matches: 30
```

#### ❌ Если есть проблема с PandaScore:
```
[Backend] ❌ PandaScore error: 401
[Backend] 🔑 API token may be invalid or expired
```
**Решение**: Проверьте `PANDASCORE_API_TOKEN` в Environment Variables

#### ❌ Если вообще не работает:
```
Error: Cannot find module 'hono'
```
**Решение**: Перезапустите деплой с очисткой кеша

---

## 5️⃣ Регион и План

### Перейдите: Dashboard → cs2-match-backend → Settings

### Рекомендуемые настройки:

- **Region**: Frankfurt (или ближайший к вашей локации)
- **Plan**: Free (для тестирования)
- **Auto-Deploy**: Включено (если хотите автоматические обновления)

---

## 6️⃣ Что делать если backend не работает

### Вариант 1: Перезапуск
1. Dashboard → cs2-match-backend
2. Нажмите **Manual Deploy**
3. Выберите **Clear build cache & deploy**
4. Дождитесь завершения (2-3 минуты)

### Вариант 2: Проверка токена
1. Dashboard → Environment
2. Убедитесь что `PANDASCORE_API_TOKEN` правильный
3. Если изменили - нажмите **Save Changes**
4. Сервис перезапустится автоматически

### Вариант 3: Проверка логов
1. Dashboard → Logs
2. Ищите ошибки (красный цвет или ❌)
3. Если видите ошибку - прочитайте что она говорит
4. Чаще всего это:
   - Missing environment variable → добавьте переменную
   - Module not found → очистите кеш и передеплойте
   - Port already in use → перезапустите сервис

---

## 7️⃣ Health Check Настройки

### Перейдите: Dashboard → cs2-match-backend → Settings → Health Check

### Рекомендуемые настройки:

- **Health Check Path**: `/api/health`
- **Health Check Interval**: 30 секунд
- **Health Check Timeout**: 10 секунд

Это позволит Render автоматически проверять что сервер работает.

---

## 8️⃣ Дополнительные API ключи (опционально)

Если в будущем получите доступ к другим API:

### BetBoom API (для реальных коэффициентов):
```
BETBOOM_API_KEY=your_betboom_key_here
```

### OpenAI API (для улучшенного AI анализа):
```
OPENAI_API_KEY=your_openai_key_here
```

**Примечание**: Сейчас эти ключи не нужны, приложение работает без них.

---

## ✅ Финальный Чеклист

Убедитесь что всё отмечено галочкой:

- [ ] `PANDASCORE_API_TOKEN` установлен в Environment Variables
- [ ] `NODE_ENV=production` установлен
- [ ] `PORT=10000` установлен
- [ ] Build Command корректный (с bun install)
- [ ] Start Command корректный (с bun run backend/server.ts)
- [ ] Сервис задеплоен (зелёный статус)
- [ ] Health check работает (открывается /api/health в браузере)
- [ ] В логах нет красных ошибок
- [ ] В логах видно "[Server] ✅ Server is running"
- [ ] Когда открываете /api/health - показывает JSON с "status": "ok"

---

## 🎉 Если все галочки стоят - ВСЁ РАБОТАЕТ!

Теперь можете:
1. Запустить приложение: `bun start --clear`
2. Увидеть "✅ Backend подключен"
3. Наслаждаться реальными данными о матчах CS2!

---

## 🆘 Если нужна помощь

### Проверьте логи Render:
- Dashboard → Logs → ищите ошибки

### Проверьте логи Expo:
- В терминале где запущен `bun start`

### Проверьте Environment Variables:
- Все 3 переменные должны быть установлены

### Если совсем не работает:
1. **Clear build cache & deploy** на Render
2. `bun start --clear` в терминале
3. Подождите 2-3 минуты
4. Проверьте /api/health в браузере

---

**Готово!** Теперь у вас есть полная инструкция по настройке Render. 🚀
