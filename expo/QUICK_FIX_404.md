# ⚡ Быстрое решение ошибки 404

## 🎯 Проблема
При проверке подключения в Settings получаете: **"Health check failed: 404"**

## ✅ Решение за 5 минут

### 1️⃣ Проверьте сервер (30 сек)
Откройте в браузере:
```
https://cs2-match-data-collector.onrender.com/api/health
```

**Если видите JSON** → Перейдите к пункту 4  
**Если 404** → Продолжайте с пункта 2

---

### 2️⃣ Render.com - Build Command
Зайдите в [Render Dashboard](https://dashboard.render.com) → Ваш сервис → Settings

**Build Command:**
```bash
curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install
```

---

### 3️⃣ Render.com - Start Command
**Start Command:**
```bash
export PATH="$HOME/.bun/bin:$PATH" && bun run backend/server.ts
```

---

### 4️⃣ Environment Variables
Добавьте в Render → Environment:

- `PORT` = `10000`
- `NODE_ENV` = `production`  
- `PANDASCORE_API_TOKEN` = ваш_токен

**Где взять токен:** https://pandascore.co/

---

### 5️⃣ Перезапустите сервис
Manual Deploy → **Clear build cache & deploy**

Подождите 3-5 минут ⏳

---

### 6️⃣ Проверьте снова
Откройте в браузере:
```
https://cs2-match-data-collector.onrender.com/api/health
```

Должны увидеть:
```json
{
  "status": "ok",
  "message": "Backend server is running",
  "timestamp": "2024-...",
  "routes": [...]
}
```

---

### 7️⃣ Тест в приложении
1. Откройте приложение
2. Settings → "Проверить подключение"
3. ✅ Зелёная галочка = всё работает!

---

## 🔴 Всё ещё 404?

### Вариант А: Неправильный URL
Render мог дать вам другой URL:

1. Render Dashboard → Ваш сервис
2. Вверху страницы найдите URL
3. Скопируйте **точный** адрес
4. Обновите в файле `env`:
   ```
   EXPO_PUBLIC_RORK_API_BASE_URL=ваш_новый_URL
   ```

### Вариант Б: Логи
Render Dashboard → Logs

Должно быть:
```
[Server] 🚀 Starting server on port 10000
[Server] ✅ Server is running on http://localhost:10000
```

Если ошибки → скопируйте их полностью

### Вариант В: Free plan "уснул"
Render бесплатный план засыпает после 15 минут.
Первый запрос может занять до 30 секунд.

**Просто подождите и попробуйте ещё раз!**

---

## 📚 Подробные гайды

Для детальной диагностики откройте:
- `TEST_BACKEND.md` - проверка работы сервера
- `RENDER_FIX_STEPS.md` - подробная инструкция

---

## ✨ Теперь работает?

После успешного подключения:
- ✅ На главной странице появятся реальные матчи
- ✅ Backend Status покажет зелёную галочку
- ✅ AI анализ будет использовать live данные

**Удачи! 🚀**
