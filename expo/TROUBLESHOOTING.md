# 🔧 Устранение проблем - Быстрая помощь

## 🚨 Частые проблемы и решения

---

### ❌ Проблема 1: "❌ Health check failed: 404"

**Причина**: Backend не отвечает или не задеплоен

**Решение**:
1. Откройте в браузере: https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health
2. Если не открывается → зайдите на Render Dashboard
3. Нажмите **Manual Deploy** → **Clear build cache & deploy**
4. Подождите 2-3 минуты
5. Проверьте снова

---

### ❌ Проблема 2: "Backend недоступен - используются демо данные"

**Причина**: Backend не доступен с клиента или ENV переменная неправильная

**Решение**:
```bash
# 1. Остановите Expo (Ctrl+C в терминале)

# 2. Проверьте файл env
cat env
# Должно быть: EXPO_PUBLIC_RORK_API_BASE_URL=https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com

# 3. Запустите заново с очисткой
bun start --clear

# 4. Проверьте в логах:
# Должно быть: [tRPC] ✅ Base URL found: https://ok3xs34tn1a0sjt9xevou...
```

---

### ❌ Проблема 3: "No matches found" / Матчи не загружаются

**Причина 1**: PandaScore API токен не установлен

**Решение**:
1. Render Dashboard → cs2-match-backend → Environment
2. Добавьте: `PANDASCORE_API_TOKEN=rX0hxTFfEhYoWjHwK2JCsMvnlL1Y66v9FBgWBvfYN9zLwpj_wy0`
3. Сохраните (сервис перезапустится)
4. Подождите 1-2 минуты

**Причина 2**: Сегодня действительно нет матчей

**Решение**:
- Попробуйте фильтр "Все" вместо "Предстоящие"
- Проверьте логи Render - должно быть количество матчей

---

### ❌ Проблема 4: "Cannot find module 'hono'" в логах Render

**Причина**: Зависимости не установились

**Решение**:
1. Render Dashboard → Settings
2. Проверьте **Build Command**:
   ```bash
   curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install
   ```
3. Нажмите **Manual Deploy** → **Clear build cache & deploy**

---

### ❌ Проблема 5: QR код не работает на телефоне

**Причина**: Tunnel не поднялся или телефон не в той же сети

**Решение**:
```bash
# 1. Остановите Expo
Ctrl+C

# 2. Запустите с tunnel
bun start

# 3. Подождите пока появится:
# "Tunnel connected."

# 4. Отсканируйте QR код снова

# Если не помогло:
# - Убедитесь что телефон в той же WiFi сети
# - Или используйте веб версию: bun start-web
```

---

### ❌ Проблема 6: "[tRPC] ❌ EXPO_PUBLIC_RORK_API_BASE_URL not set"

**Причина**: ENV переменная не загрузилась

**Решение**:
```bash
# 1. Проверьте файл env
cat env

# 2. Должно быть ТОЧНО так:
EXPO_PUBLIC_RORK_API_BASE_URL=https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com

# 3. Если неправильно - исправьте:
# (уберите пробелы, кавычки, всё должно быть на одной строке)

# 4. Перезапустите:
bun start --clear
```

---

### ❌ Проблема 7: "TypeError: Failed to fetch"

**Причина**: Нет интернета или CORS ошибка

**Решение**:
1. **Проверьте интернет** на компьютере/телефоне
2. **Проверьте что backend работает**: откройте /api/health в браузере
3. **На Web версии могут быть CORS проблемы** - используйте мобильное приложение
4. **Проверьте firewall** - может блокировать запросы

---

### ❌ Проблема 8: PandaScore возвращает 401/403

**Причина**: Токен неправильный или истёк

**Решение**:
1. Проверьте токен на Render (Environment Variables)
2. Зайдите на https://pandascore.co и получите новый токен
3. Обновите `PANDASCORE_API_TOKEN` на Render
4. **ХОРОШАЯ НОВОСТЬ**: Даже если PandaScore не работает, приложение переключится на HLTV API

---

### ❌ Проблема 9: Matches есть но нет деталей (scores, logos и т.д.)

**Причина**: Источник данных не предоставляет полную информацию

**Решение**:
- Это нормально для некоторых источников (HLTV)
- PandaScore предоставляет более полные данные
- Убедитесь что `PANDASCORE_API_TOKEN` работает (см. проблему 8)

---

### ❌ Проблема 10: Live матчи не обновляются

**Причина**: Автообновление не работает

**Решение**:
1. Проверьте что backend подключен (зелёный индикатор)
2. Live матчи обновляются каждые 3 минуты автоматически
3. Можно обновить вручную кнопкой 🔄 в правом верхнем углу

---

## 🔍 Как понять что работает правильно?

### ✅ В приложении должно быть:
```
✅ Backend подключен - реальные данные
✅ Реальные данные (PandaScore)
```
или
```
✅ Backend подключен - реальные данные
✅ Реальные данные (HLTV)
```

### ✅ В логах Expo должно быть:
```
[tRPC] ✅ Base URL found: https://ok3xs34tn1a0sjt9xevou...
[tRPC] 📊 Response status: 200
[MatchesContext] ✅ Backend available - using real matches: 25
```

### ✅ В логах Render должно быть:
```
[Server] ✅ Server is running on http://localhost:10000
[Backend] ✅ PandaScore: 25 matches
[Backend] ✅ Total unique matches: 30
```

---

## 📋 Быстрая диагностика (5 шагов)

### Шаг 1: Проверьте backend
```bash
curl https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health
```
✅ Работает: показывает JSON с "status": "ok"
❌ Не работает: ошибка или ничего не показывает → идите на Render, передеплойте

### Шаг 2: Проверьте ENV
```bash
cat env
```
✅ Правильно: `EXPO_PUBLIC_RORK_API_BASE_URL=https://ok3xs34tn1a0sjt9xevou...`
❌ Неправильно: исправьте файл env

### Шаг 3: Проверьте PandaScore токен на Render
Render → Environment → `PANDASCORE_API_TOKEN` должен быть установлен

### Шаг 4: Перезапустите Expo
```bash
bun start --clear
```

### Шаг 5: Проверьте индикатор в приложении
✅ "Backend подключен" - всё ок!
❌ "Backend недоступен" - повторите шаги 1-4

---

## 🆘 Если ничего не помогает

### Полный сброс (гарантированно помогает):

#### 1. На Render:
- Dashboard → Manual Deploy → Clear build cache & deploy
- Подождите 3 минуты

#### 2. В проекте:
```bash
# Остановите Expo
Ctrl+C

# Проверьте env файл
cat env
# Должно быть: EXPO_PUBLIC_RORK_API_BASE_URL=https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com

# Удалите кеши
rm -rf .expo
rm -rf node_modules/.cache

# Запустите заново
bun start --clear
```

#### 3. Проверка:
```bash
# В браузере откройте:
https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health

# Должно показать JSON с "status": "ok"
```

Если после этого не работает - значит проблема в самом Render (их сервера), подождите 10-15 минут.

---

## 💡 Полезные команды для диагностики

```bash
# Проверить backend
curl https://ok3xs34tn1a0sjt9xevou.rork-app-backend.onrender.com/api/health

# Проверить ENV файл
cat env

# Проверить что Expo видит ENV
bun start --clear
# В логах ищите: [tRPC] 🌐 EXPO_PUBLIC_RORK_API_BASE_URL =

# Очистить все кеши
rm -rf .expo node_modules/.cache

# Полная переустановка зависимостей
rm -rf node_modules
bun install

# Проверить типы TypeScript
npx tsc --noEmit

# Проверить линтер
bun lint
```

---

## 📞 Контакты для поддержки

- **Логи Render**: Dashboard → Logs (ищите ошибки с ❌)
- **Логи Expo**: В терминале (ищите [tRPC], [MatchesContext])
- **Документация PandaScore**: https://pandascore.co/docs
- **Документация Expo**: https://docs.expo.dev

---

## ✅ Всё работает когда:

1. ✅ `/api/health` открывается в браузере
2. ✅ В Render логах нет ошибок
3. ✅ В приложении показывает "Backend подключен"
4. ✅ Матчи загружаются
5. ✅ Можно открыть детали матча

**Если все 5 пунктов ✅ - поздравляю, всё отлично работает! 🎉**
