# 🔧 Решение проблемы Health Check Failed: 404

## Причины ошибки

Ошибка "Health check failed: 404" означает, что приложение не может подключиться к backend серверу по адресу `/api/health`.

## ✅ Пошаговое решение

### Шаг 1: Проверка сервера
Откройте в браузере: https://cs2-match-data-collector.onrender.com/api/health

**Результаты:**
- ✅ Видите JSON `{"status":"ok"}` → Сервер работает, переходите к Шагу 4
- ❌ 404 или другая ошибка → Продолжайте со Шага 2

### Шаг 2: Настройки Render.com

Зайдите в [Render Dashboard](https://dashboard.render.com) → Выберите ваш сервис `cs2-match-backend`

#### Settings → Build & Deploy

**Build Command:**
```bash
curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install
```

**Start Command:**
```bash
export PATH="$HOME/.bun/bin:$PATH" && bun run backend/server.ts
```

**ВАЖНО:** Убедитесь, что команды скопированы ПОЛНОСТЬЮ, без переноса строк внутри команды!

### Шаг 3: Environment Variables

В Render Dashboard → Environment:

Добавьте/проверьте переменные:
- `PORT` = `10000` (или автоматически от Render)
- `NODE_ENV` = `production`
- `PANDASCORE_API_TOKEN` = ваш токен PandaScore API

**⚠️ Без PANDASCORE_API_TOKEN сервер может не работать правильно!**

Где взять токен:
1. Зарегистрируйтесь на https://pandascore.co/
2. Создайте API токен в личном кабинете
3. Добавьте в Environment Variables

### Шаг 4: Перезапуск

1. В Render Dashboard → Manual Deploy
2. **Clear build cache & deploy** (важно!)
3. Подождите 3-5 минут для деплоя

### Шаг 5: Проверка логов

В Render Dashboard → Logs, должны увидеть:
```
[Server] 🚀 Starting server on port 10000
[Server] ✅ Server is running on http://localhost:10000
[Server] 📡 Health check: http://localhost:10000/api/health
[Server] 🔌 tRPC endpoint: http://localhost:10000/api/trpc
```

**Если видите ошибки:**
- `Cannot find module` → Перезапустите с Clear build cache
- `PANDASCORE_API_TOKEN is not defined` → Добавьте токен в Environment Variables
- Другие ошибки → Скопируйте полный лог ошибки

### Шаг 6: Проверка URL в приложении

Файл `env` должен содержать правильный URL:
```
EXPO_PUBLIC_RORK_API_BASE_URL=https://cs2-match-data-collector.onrender.com
```

**БЕЗ слеша в конце!**

### Шаг 7: Тест в приложении

1. Откройте приложение
2. Перейдите в Settings (Настройки)
3. Нажмите "Проверить подключение"
4. Должна появиться зелёная галочка ✅

## 🆘 Если ничего не помогло

### Альтернатива 1: Проверьте URL вручную

Откройте в браузере каждый URL:
- https://cs2-match-data-collector.onrender.com/
- https://cs2-match-data-collector.onrender.com/api
- https://cs2-match-data-collector.onrender.com/api/health

Какой из них работает? Это поможет понять проблему.

### Альтернатива 2: Используйте другой домен

Возможно, Render изменил ваш URL. Проверьте в Dashboard:
- Settings → найдите поле "URL" или "Domain"
- Скопируйте **точный** URL (например: `https://cs2-match-backend-xxx.onrender.com`)
- Обновите файл `env`

### Альтернатива 3: Временно используйте mock данные

В приложении уже есть fallback на демо данные. Если backend не критичен сейчас, можете временно использовать демо режим.

## 📊 Диагностика

Чтобы помочь с проблемой, предоставьте:
1. ✅ URL, который открываете в браузере
2. ✅ Что видите в браузере (скриншот)
3. ✅ Логи с Render (последние 50 строк)
4. ✅ Скриншот Environment Variables в Render
5. ✅ Содержимое файла `env`

## 🎯 Быстрая проверка

Выполните эти команды в терминале проекта:

```bash
# Проверка URL
curl https://cs2-match-data-collector.onrender.com/api/health

# Должен вернуть:
# {"status":"ok","message":"Backend server is running","timestamp":"..."}
```

Если не работает → проблема в backend на Render
Если работает → проблема в настройках приложения

## 📝 Что НЕ нужно делать

❌ Не меняйте runtime с `node` на что-то другое
❌ Не добавляйте `npm install` в команды (используем bun)
❌ Не ставьте слеш `/` в конце URL
❌ Не забывайте про `export PATH` в командах
