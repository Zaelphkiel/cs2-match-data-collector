# 🧪 Быстрый тест Backend

## Метод 1: Браузер (самый простой)

Откройте в браузере эти URL по очереди:

### 1. Корневой endpoint
```
https://cs2-match-data-collector.onrender.com/
```
**Ожидаемый результат:**
```json
{
  "status": "ok",
  "message": "CS2 Match API is running",
  "timestamp": "2024-..."
}
```

### 2. API endpoint
```
https://cs2-match-data-collector.onrender.com/api
```
**Ожидаемый результат:**
```json
{
  "status": "ok",
  "message": "API endpoint is working",
  "timestamp": "2024-..."
}
```

### 3. Health check endpoint ⭐ (ГЛАВНЫЙ)
```
https://cs2-match-data-collector.onrender.com/api/health
```
**Ожидаемый результат:**
```json
{
  "status": "ok",
  "message": "Backend server is running",
  "timestamp": "2024-...",
  "routes": [
    "/api/trpc/example.hi",
    "/api/trpc/matches.all"
  ]
}
```

## Метод 2: Terminal / CMD

```bash
curl https://cs2-match-data-collector.onrender.com/api/health
```

## Метод 3: Postman / Insomnia

1. Создайте GET запрос
2. URL: `https://cs2-match-data-collector.onrender.com/api/health`
3. Send

## 🔴 Что делать, если получаете 404?

### Вариант А: Неправильный URL

Возможно, ваш сервис на Render имеет другой URL:

1. Зайдите в [Render Dashboard](https://dashboard.render.com)
2. Откройте ваш сервис
3. В верху страницы найдите URL (обычно вида `https://xxx.onrender.com`)
4. Скопируйте **точный** URL
5. Обновите в файле `env`:
   ```
   EXPO_PUBLIC_RORK_API_BASE_URL=ваш_точный_URL
   ```

### Вариант Б: Сервис не запущен

1. Зайдите в Render Dashboard
2. Проверьте статус сервиса (должен быть зелёный "Live")
3. Если статус "Deploy failed" или "Suspended":
   - Нажмите "Manual Deploy"
   - Clear build cache & deploy
   - Подождите 3-5 минут

### Вариант В: Неправильная конфигурация

Проверьте в Render Dashboard → Settings:

**Build Command должен быть:**
```bash
curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install
```

**Start Command должен быть:**
```bash
export PATH="$HOME/.bun/bin:$PATH" && bun run backend/server.ts
```

## 🟡 Что делать, если получаете другую ошибку?

### CORS Error
Это нормально в браузере. CORS настроены для работы с приложением.
Попробуйте в приложении Settings → Проверить подключение.

### Timeout / Took too long
Render на бесплатном плане "засыпает" после 15 минут неактивности.
Первый запрос может занять до 30 секунд - это нормально!
Просто подождите и обновите страницу.

### 500 Internal Server Error
1. Проверьте логи в Render Dashboard → Logs
2. Найдите ошибку (красный текст)
3. Скорее всего не хватает Environment Variables:
   - Добавьте `PANDASCORE_API_TOKEN`

## ✅ Чеклист успешного запуска

- [ ] Сервис показывает статус "Live" в Render
- [ ] URL `/` возвращает JSON со статусом "ok"
- [ ] URL `/api` возвращает JSON со статусом "ok"  
- [ ] URL `/api/health` возвращает JSON с routes
- [ ] В логах видно "Server is running on http://localhost:10000"
- [ ] Environment Variables настроены (минимум PORT и NODE_ENV)
- [ ] Файл `env` содержит правильный EXPO_PUBLIC_RORK_API_BASE_URL

## 🎯 После успешной проверки

1. Откройте приложение
2. Перейдите в Settings
3. Нажмите "Проверить подключение"
4. Должна появиться зелёная галочка ✅

Если галочка зелёная - всё работает!
Если красный крестик - смотрите логи в Metro Bundler.
