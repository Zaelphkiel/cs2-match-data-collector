# ⚡ Быстрая проверка приложения

## 🔗 Актуальный URL Backend
```
https://cs2-match-data-collector.onrender.com
```
✅ **Обновлено в файле `env`**

---

## 🚀 Запуск приложения

### 1. Запустить приложение
```bash
bun start
```

### 2. Открыть в браузере (web версия)
Нажать `w` в терминале или перейти по URL из консоли

### 3. Открыть на телефоне iOS (Expo Go)
1. Установить **Expo Go** из App Store
2. Отсканировать QR код из терминала
3. Приложение откроется в Expo Go

---

## ✅ Проверка работоспособности

### 1. Backend доступен
Открыть в браузере:
```
https://cs2-match-data-collector.onrender.com/api/health
```

**Должно вернуть:**
```json
{
  "status": "ok",
  "message": "Backend server is running"
}
```

### 2. Приложение подключено к backend
В приложении должен быть зеленый статус:
```
✅ Backend подключен - реальные данные
```

Если желтый статус:
```
⚠️ Backend недоступен - используются демо данные
```
Значит backend спит или недоступен. Подождите 30-60 секунд и нажмите кнопку обновления.

### 3. Матчи загружаются
- Должны отображаться реальные CS2 матчи из PandaScore или HLTV
- Под фильтрами должен быть бадж: `✅ Реальные данные (PandaScore)` или `✅ Реальные данные (HLTV)`

### 4. Фильтры работают
Попробовать все фильтры:
- ✅ Все
- ✅ Предстоящие
- ✅ Live
- ✅ Завершенные

### 5. Детали матча открываются
- Нажать на любой матч
- Должна открыться страница с деталями матча

### 6. AI анализ работает
- Открыть любой матч
- Найти кнопку "Полный анализ" или "AI анализ"
- Должен сгенерироваться детальный анализ на русском языке

### 7. Live updates (для live матчей)
- Открыть live матч
- Счет должен обновляться автоматически каждые 10 секунд

---

## 🐛 Если что-то не работает

### Backend недоступен (404)
**Проблема:** При запуске приложения показывает желтый статус

**Решение:**
1. Открыть https://cs2-match-data-collector.onrender.com/api/health
2. Если не открывается:
   - Проверить Render.com - статус сервиса
   - Подождать 1-2 минуты (cold start)
   - Проверить логи на Render
3. Если открывается, но приложение не подключается:
   - Проверить файл `env` - должен быть правильный URL
   - Перезапустить приложение: `bun start --clear`

### Нет матчей / только mock данные
**Проблема:** Матчи не загружаются или показываются демо данные

**Решение:**
1. Проверить backend статус (см. выше)
2. Проверить API токен PandaScore в настройках Render:
   ```
   PANDASCORE_API_TOKEN=rX0hxTFfEhYoWjHwK2JCsMvnlL1Y66v9FBgWBvfYN9zLwpj_wy0
   ```
3. Проверить логи backend на Render
4. Нажать кнопку обновления в приложении

### AI анализ не работает
**Проблема:** При нажатии на анализ ничего не происходит или ошибка

**Решение:**
1. Проверить интернет соединение
2. Проверить консоль (DevTools) на ошибки
3. Попробовать другой матч
4. Перезапустить приложение

### Приложение не открывается в Expo Go
**Проблема:** QR код не сканируется или приложение не загружается

**Решение:**
1. Убедиться что телефон и компьютер в одной WiFi сети
2. Попробовать с tunnel: `bun start --tunnel`
3. Проверить что Expo Go установлен
4. Проверить что Metro bundler запущен

---

## 🔍 Проверка Environment Variables на Render

Зайти в Render.com → Ваш сервис → Environment

**Должны быть установлены:**
```
NODE_ENV = production
PORT = 10000
PANDASCORE_API_TOKEN = rX0hxTFfEhYoWjHwK2JCsMvnlL1Y66v9FBgWBvfYN9zLwpj_wy0
```

**Если отсутствуют - добавить вручную**

---

## 📱 Тестирование на телефоне

### iOS (Expo Go)
1. App Store → найти "Expo Go" → установить
2. На компьютере: `bun start`
3. Отсканировать QR код в Expo Go
4. Приложение загрузится

### Если не работает
- Попробовать с tunnel: `bun start --tunnel`
- Проверить WiFi соединение
- Попробовать ручной ввод URL из консоли

---

## 🎯 Быстрая диагностика

### 1. Backend health check
```bash
curl https://cs2-match-data-collector.onrender.com/api/health
```

### 2. tRPC endpoint test
```bash
curl -X POST https://cs2-match-data-collector.onrender.com/api/trpc/matches.all \
  -H "Content-Type: application/json" \
  -d '{"json":{}}'
```

### 3. Проверка логов
- **Render:** Dashboard → Logs
- **Metro:** Терминал где запущен `bun start`
- **Browser:** DevTools → Console

---

## ✅ Все работает если:

1. ✅ Backend health check возвращает `{"status":"ok"}`
2. ✅ В приложении зеленый статус "Backend подключен"
3. ✅ Матчи загружаются с PandaScore/HLTV (не mock)
4. ✅ Фильтры переключают матчи
5. ✅ Детали матча открываются
6. ✅ AI анализ генерируется
7. ✅ Live матчи обновляются
8. ✅ Коэффициенты отображаются

---

## 🆘 Если ничего не помогло

1. Проверить логи на Render (может быть ошибка при запуске)
2. Проверить Build Command и Start Command на Render
3. Перезапустить сервис на Render (Manual Deploy)
4. Очистить кэш и перезапустить приложение:
   ```bash
   bun start --clear
   ```
5. Проверить все Environment Variables на Render

---

## 📋 Команды на Render

### Build Command
```bash
curl -fsSL https://bun.sh/install | bash
export PATH="$HOME/.bun/bin:$PATH"
bun install
```

### Start Command
```bash
export PATH="$HOME/.bun/bin:$PATH"
bun run backend/server.ts
```

---

## 🎉 Готово!

Приложение настроено и готово к использованию!

**Все компоненты:**
- ✅ Backend на Render
- ✅ PandaScore API
- ✅ HLTV API (fallback)
- ✅ AI анализ (Rork Toolkit)
- ✅ Live updates
- ✅ Коэффициенты букмекеров (BetBoom, 1xBet, Parimatch)
- ✅ Mobile поддержка (iOS Expo Go)

**Источники данных работают:**
- ✅ PandaScore - актуальные CS2 матчи
- ✅ HLTV - резервный источник
- ✅ AI - глубокий анализ на русском
- ✅ БК - реальные коэффициенты

Наслаждайтесь приложением! 🎮🚀
