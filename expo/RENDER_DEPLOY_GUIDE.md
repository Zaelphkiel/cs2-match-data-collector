# 🚀 Пошаговая инструкция развертывания на Render

## Шаг 1: Подготовка проекта (УЖЕ ГОТОВО ✅)

Я уже создал все необходимые файлы:
- ✅ `render.yaml` - конфигурация для Render
- ✅ `backend/server.ts` - сервер для production
- ✅ Установлен `@hono/node-server` пакет

## Шаг 2: Создание аккаунта на Render

1. **Перейдите на сайт**: https://render.com
2. **Нажмите "Get Started"** в правом верхнем углу
3. **Зарегистрируйтесь через GitHub** (это самый простой способ)
   - Нажмите "Sign up with GitHub"
   - Авторизуйте Render доступ к вашему GitHub аккаунту

## Шаг 3: Загрузка кода на GitHub

Если ваш проект еще не на GitHub:

1. **Создайте новый репозиторий на GitHub**:
   - Перейдите на https://github.com/new
   - Назовите репозиторий, например: `cs2-match-tracker`
   - Выберите "Private" (приватный) или "Public" (публичный)
   - НЕ добавляйте README, .gitignore или лицензию (у вас уже есть)
   - Нажмите "Create repository"

2. **Загрузите код** (выполните в терминале в папке проекта):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ-ЮЗЕРНЕЙМ/cs2-match-tracker.git
   git push -u origin main
   ```

## Шаг 4: Создание Web Service на Render

1. **Войдите в Dashboard Render**: https://dashboard.render.com

2. **Нажмите "New +"** в правом верхнем углу

3. **Выберите "Web Service"**

4. **Подключите репозиторий**:
   - Нажмите "Connect" напротив вашего репозитория
   - Если не видите репозиторий, нажмите "Configure account" и дайте доступ

5. **Заполните настройки**:

   **Name**: `cs2-match-backend` (или любое другое имя)
   
   **Region**: `Frankfurt (EU Central)` (выберите ближайший регион)
   
   **Branch**: `main`
   
   **Root Directory**: оставьте пустым
   
   **Runtime**: `Node`
   
   **Build Command**: 
   ```
   bun install
   ```
   
   **Start Command**:
   ```
   bun run backend/server.ts
   ```
   
   **Plan**: `Free` (бесплатный план)

6. **Добавьте переменные окружения**:
   - Прокрутите вниз до секции "Environment Variables"
   - Нажмите "Add Environment Variable"
   - Добавьте:
     - Key: `NODE_ENV`, Value: `production`
     - Key: `PORT`, Value: `10000`

7. **Нажмите "Create Web Service"**

8. **Дождитесь деплоя** (обычно 2-5 минут):
   - Вы увидите логи сборки и запуска
   - Когда статус станет "Live" - сервер готов! 🎉

## Шаг 5: Получение URL вашего сервера

1. После успешного деплоя, вы увидите URL типа:
   ```
   https://cs2-match-backend.onrender.com
   ```

2. **Скопируйте этот URL** - он вам понадобится!

3. **Проверьте работу сервера**:
   - Откройте в браузере: `https://ВАШ-URL.onrender.com/api/health`
   - Вы должны увидеть JSON с `"status": "ok"`

## Шаг 6: Настройка приложения

**ВАЖНО**: Теперь вам нужно добавить URL вашего сервера в приложение.

Есть 2 способа:

### Способ 1: Через Rork интерфейс (РЕКОМЕНДУЕТСЯ)

1. В Rork интерфейсе найдите настройки проекта
2. Найдите "Environment Variables" или "Backend URL"
3. Добавьте переменную:
   - Name: `EXPO_PUBLIC_RORK_API_BASE_URL`
   - Value: `https://ВАШ-URL.onrender.com` (без `/api/trpc` в конце!)

### Способ 2: Через .env файл (если работаете локально)

Создайте файл `.env` в корне проекта:
```
EXPO_PUBLIC_RORK_API_BASE_URL=https://ВАШ-URL.onrender.com
```

## Шаг 7: Перезапуск приложения

1. **Остановите приложение** (Ctrl+C в терминале)
2. **Запустите снова**: `bun start`
3. **Перезагрузите приложение** на телефоне (встряхните телефон → Reload)

## Шаг 8: Проверка работы

1. Откройте приложение
2. Вы должны увидеть:
   - ✅ "Подключено к серверу"
   - ✅ Матчи загружаются
   - ✅ Нет ошибок 404

## 🎯 Готово! Теперь ваше приложение работает из любой точки мира!

---

## 🔧 Устранение проблем

### Проблема: Сервер "спит" (холодный старт)

**Причина**: Бесплатный план Render "усыпляет" сервер после 15 минут неактивности.

**Решение**:
1. Первый запрос может занять 30-60 секунд (сервер "просыпается")
2. Добавьте индикатор загрузки в приложении
3. Или используйте платный план ($7/мес) для постоянной работы

### Проблема: "JSON Parse error"

**Причина**: Неправильный URL или сервер не запустился.

**Решение**:
1. Проверьте логи на Render Dashboard
2. Убедитесь что URL правильный (без лишних слэшей)
3. Проверьте что в URL нет `/api/trpc` в конце

### Проблема: "404 Not Found"

**Причина**: Деплой не завершился или есть ошибки.

**Решение**:
1. Проверьте логи деплоя на Render
2. Убедитесь что статус "Live"
3. Проверьте `/api/health` endpoint

---

## 📊 Мониторинг

В Render Dashboard вы можете:
- Видеть логи в реальном времени
- Мониторить использование ресурсов
- Настроить автоматический редеплой при push в GitHub
- Смотреть историю деплоев

---

## 💰 Стоимость

**Бесплатный план**:
- ✅ 750 часов в месяц
- ✅ Автоматический SSL
- ✅ Глобальный CDN
- ⚠️ Сервер "засыпает" после 15 минут
- ⚠️ 512 MB RAM

**Платный план** ($7/мес):
- ✅ Сервер всегда активен
- ✅ Больше ресурсов
- ✅ Приоритетная поддержка

---

## 🚀 Автоматические обновления

После настройки, при каждом push в GitHub:
1. Render автоматически запустит билд
2. Выполнит тесты (если есть)
3. Задеплоит новую версию
4. Всё автоматически! 🎉

---

**Если что-то не работает - дайте знать, я помогу разобраться!** 👨‍💻
