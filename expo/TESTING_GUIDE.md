# 🧪 Руководство по тестированию CS2 Match Tracker

## ✅ Статус проверки

Все компоненты приложения проверены и настроены правильно:

### 1. Backend настроен ✅
- **URL**: https://cs2-match-collector.onrender.com
- **Здоровье**: `/api/health`
- **tRPC endpoint**: `/api/trpc`
- **Роуты**: 
  - `matches.all` - получение всех матчей
  - `example.hi` - тестовый роут

### 2. Источники данных ✅
Backend получает данные из двух источников:

#### PandaScore API
- **URL**: https://api.pandascore.co/csgo/matches
- **Токен**: Настроен в `backend/trpc/routes/matches/fetch-all/route.ts`
- **Лимит**: 50 матчей
- **Данные**: Реальные матчи CS2 с логотипами команд, счетами, статусами

#### HLTV API
- **URL**: https://hltv-api.vercel.app/api/matches.json
- **Токен**: Не требуется
- **Данные**: Реальные матчи CS2 с информацией о турнирах

### 3. Frontend интеграция ✅

#### Переменные окружения
```env
EXPO_PUBLIC_RORK_API_BASE_URL=https://cs2-match-collector.onrender.com
```

#### Contexts
- **MatchesContext**: Управляет загрузкой матчей, фильтрами, кэшированием
- **NotificationsContext**: Управляет уведомлениями о матчах
- **AIAnalysisContext**: AI-анализ матчей

#### Компоненты
- **MatchCard**: Отображение карточки матча
- **BackendStatus**: Индикатор подключения к backend
- **Settings**: Настройки и диагностика

---

## 🧪 Как протестировать

### Шаг 1: Проверка переменных окружения
```bash
# Убедитесь что переменная установлена
cat env

# Должно быть:
# EXPO_PUBLIC_RORK_API_BASE_URL=https://cs2-match-collector.onrender.com
```

### Шаг 2: Запуск приложения
```bash
# Очистить кэш и запустить
bun start --clear

# Или для web
bun start-web
```

### Шаг 3: Тестирование в приложении

#### 📱 На главной странице (Матчи)
1. **Проверьте индикатор Backend Status**
   - Зеленый = "Backend подключен - реальные данные"
   - Желтый = "Backend недоступен - используются демо данные"

2. **Проверьте источник данных**
   - Внизу фильтров должен быть бейдж "✅ Реальные данные (PandaScore)" или "(HLTV)"

3. **Проверьте филь��ры**
   - Все / Предстоящие / Live / Завершенные
   - Должны работать без задержек

4. **Проверьте обновление**
   - Нажмите кнопку обновления в хедере (справа)
   - Должна появиться анимация загрузки

#### ⚙️ На странице настроек
1. **Тест подключения**
   - Перейдите в раздел "Диагностика"
   - Нажмите "Проверить подключение"
   - Должна появиться галочка ✅ и сообщение "Сервер доступен!"

2. **Проверка логов в консоли**
   ```
   [Settings] 🔍 Testing connection to backend...
   [Settings] 🌐 Testing connection to: https://cs2-match-collector.onrender.com
   [Settings] ✅ Health check passed: { status: "ok", ... }
   [Settings] ✅ Connection test successful, matches: XX
   ```

---

## 🔍 Диагностика проблем

### Проблема: Backend недоступен

**Симптомы:**
- Желтый индикатор "Backend недоступен"
- Показываются демо данные

**Решение:**
1. Проверьте что backend запущен на Render:
   ```bash
   curl https://cs2-match-collector.onrender.com/api/health
   ```

2. Должен вернуть:
   ```json
   {
     "status": "ok",
     "message": "Backend server is running",
     "timestamp": "2025-01-XX...",
     "routes": [...]
   }
   ```

3. Если backend спит (Render free tier):
   - Первый запрос может занять 30-60 секунд
   - Перезагрузите приложение через 1 минуту

### Проблема: Ошибка JSON parsing

**Симптомы:**
- `TRPCClientError: Unexpected non-whitespace character after JSON`

**Решение:**
✅ Исправлено! Теперь в Settings:
1. Сначала проверяется `/api/health` (обычный JSON)
2. Затем проверяется tRPC endpoint (SuperJSON)

### Проблема: Переменная окружения не работает

**Симптомы:**
- `EXPO_PUBLIC_RORK_API_BASE_URL not set`
- Backend URL = undefined

**Решение:**
1. Убедитесь что файл `env` существует в корне проекта:
   ```bash
   ls -la env
   ```

2. Проверьте содержимое:
   ```bash
   cat env
   ```

3. Перезапустите Expo с очисткой кэша:
   ```bash
   bun start --clear
   ```

4. Проверьте в логах при старте:
   ```
   [tRPC] 🔧 Checking environment variables...
   [tRPC] 🌐 EXPO_PUBLIC_RORK_API_BASE_URL = https://cs2-match-collector.onrender.com
   ```

---

## 📊 Проверка данных

### Какие данные должны загружаться?

#### От PandaScore:
- ✅ Реальные турниры (ESL, BLAST, IEM)
- ✅ Реальные команды с логотипами
- ✅ Актуальные счета для Live и Finished матчей
- ✅ Статусы: upcoming / live / finished

#### От HLTV:
- ✅ Реальные матчи
- ✅ Актуальные турниры
- ✅ Live статусы

### Проверка в консоли:
```
[Backend] 🚀 START: Fetching matches from multiple sources...
[Backend] 🔍 Trying PandaScore API...
[Backend] ✅ PandaScore: XX matches
[Backend] 🔍 Trying HLTV API...
[Backend] ✅ HLTV: XX matches
[Backend] ✅ Total unique matches: XX
[Backend] 📋 Status breakdown: upcoming=X, live=X, finished=X
```

---

## 🚀 Деплой на Render (если нужно обновить)

### Build Command:
```bash
bun install
```

### Start Command:
```bash
bun run backend/server.ts
```

### Environment Variables:
```
PORT=10000
NODE_ENV=production
```

---

## ✨ Что исправлено в этой версии

1. **Исправлен JSON parsing error в Settings**
   - Сначала проверяем /api/health (plain JSON)
   - Затем проверяем tRPC (SuperJSON)
   - Добавлена подробная диагностика ошибок

2. **Улучшена обработка ошибок**
   - Детальное логирование в консоль
   - Понятные сообщения об ошибках
   - Fallback на демо данные

3. **Добавлены индикаторы состояния**
   - BackendStatus компонент
   - Источник данных (PandaScore/HLTV)
   - Кнопка обновления с feedback

4. **Оптимизирована загрузка**
   - Проверка backend только один раз при старте
   - Auto-refresh каждые 3 минуты (если backend доступен)
   - Кэширование на 30 секунд

---

## 📝 Следующие шаги

1. ✅ Backend работает и возвращает реальные данные
2. ✅ Frontend подключается к backend
3. ✅ Проверка подключения работает
4. ✅ Фильтры и UI работают

### Можно улучшить:
- [ ] Добавить больше источников данных
- [ ] Кэширование в AsyncStorage
- [ ] Push-уведомления о начале матчей
- [ ] Deep linking для матчей
- [ ] Offline mode с сохранением последних данных

---

## 💡 Полезные команды

```bash
# Проверить backend
curl https://cs2-match-collector.onrender.com/api/health

# Проверить tRPC
curl https://cs2-match-collector.onrender.com/api/trpc/matches.all

# Очистить кэш Expo
bun start --clear

# Посмотреть логи Render
# Перейти на https://dashboard.render.com -> ваш сервис -> Logs

# Перезапустить Expo в режиме разработки
# Нажать 'r' в терминале где запущен Expo
```

---

## 🎯 Критерии успешного тестирования

✅ Приложение запускается без ошибок  
✅ Backend Status показывает "Backend подключен"  
✅ Показываются реальные матчи (не демо)  
✅ Фильтры работают  
✅ Проверка подключения в Settings работает  
✅ Матчи обновляются при нажатии кнопки обновления  
✅ Нет ошибок в консоли  
✅ Детали матча открываются корректно  

---

**Версия:** 1.0.0  
**Дата:** 2025-01-XX  
**Статус:** ✅ Все компоненты проверены и работают
