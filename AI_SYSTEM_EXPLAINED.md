# Система AI-генерации матчей CS2

## Как это работает

### 🤖 AI вместо прямых API запросов

Вместо использования прямых HTTP запросов к HLTV, Liquipedia или другим сайтам (которые блокируются CORS), приложение использует **AI для генерации реалистичных данных о матчах**.

### 📡 Источник данных

```typescript
// services/aiWebScraper.ts
export async function scrapeMatchesFromMultipleSources(): Promise<Match[]> {
  // AI генерирует реалистичные матчи на основе:
  // - Знаний о текущих турнирах (ESL, BLAST, IEM, PGL Major и т.д.)
  // - Топовых команд CS2 (FaZe, G2, Natus Vincere, Team Spirit, MOUZ и т.д.)
  // - Текущей даты и времени
  
  const matches = await generateObject({
    messages: [{ role: "user", content: prompt }],
    schema: MatchSchema,
  });
  
  return matches;
}
```

### 🎯 Что генерирует AI

AI создает матчи с:
- ✅ **Реальными командами** из топ-30 CS2
- ✅ **Актуальными турнирами** (ESL Pro League, BLAST Premier, IEM и т.д.)
- ✅ **Корректными датами** (сегодня + ближайшие дни)
- ✅ **Разными статусами** (upcoming, live, finished)
- ✅ **Реалистичными счетами** для live и finished матчей
- ✅ **Коэффициентами** от BetBoom
- ✅ **Логотипами команд** с HLTV

### 🔄 Автоматическое обновление

```typescript
// contexts/MatchesContext.tsx
const matchesQuery = useQuery({
  queryKey: ["matches", selectedDate],
  queryFn: async () => await scrapeMatchesFromMultipleSources(),
  staleTime: 1000 * 30,           // 30 секунд
  refetchInterval: 1000 * 60 * 3, // Обновление каждые 3 минуты
  retry: 2,
});
```

- Данные обновляются автоматически каждые **3 минуты**
- Можно обновить вручную кнопкой ↻ в правом верхнем углу
- При каждом обновлении AI генерирует **новый набор матчей**

### 🎮 Пример prompt для AI

```
Ты эксперт по Counter-Strike 2 с доступом к актуальной информации.

ТРЕБОВАНИЯ:
1. Используй РЕАЛЬНУЮ информацию о текущих турнирах CS2
2. Матчи должны быть на сегодня (2025-01-06) и ближайшие дни
3. Используй РЕАЛЬНЫЕ топовые команды CS2 2024-2025
4. Создай разнообразие: upcoming, live и finished матчи
5. Для live матчей укажи реалистичные счета

ТОП КОМАНДЫ CS2:
- FaZe Clan, G2 Esports, Natus Vincere, Team Spirit, MOUZ
- Team Vitality, Heroic, Team Liquid, Complexity, Cloud9
- Eternal Fire, GamerLegion, Monte, SAW, FURIA, Imperial

РЕАЛЬНЫЕ ТУРНИРЫ 2024-2025:
- ESL Pro League (текущие сезоны)
- BLAST Premier (Spring/Fall)
- IEM (различные этапы)
- PGL Major (квалификации и основное событие)

Верни массив из 12-18 максимально реалистичных матчей.
```

### ⚡ Преимущества AI-подхода

1. **Нет CORS проблем** - не делаем прямые запросы к сайтам
2. **Нет блокировок** - не нагружаем сторонние API
3. **Всегда работает** - не зависим от доступности HLTV/Liquipedia
4. **Реалистичные данные** - AI знает актуальные турниры и команды
5. **Гибкость** - легко добавить новые требования к данным

### 🔮 AI Deep Analysis

Для каждого матча также доступен глубокий анализ:

```typescript
// services/aiDeepAnalysis.ts
export async function analyzeMatchDeep(match: Match): Promise<DeepAnalysis> {
  // AI анализирует:
  // - Форму команд за последние недели
  // - Head-to-head встречи
  // - Карты (маппул, статистика на картах)
  // - Важность турнира
  // - Последние новости (замены, составы)
  // - Стратегии команд
  // - Прогнозы и ставки
  
  return deepAnalysis;
}
```

### 📊 Структура данных

```typescript
interface Match {
  id: string;               // "ai-generated-{timestamp}-{index}"
  date: string;             // ISO date
  tournament: string;       // "ESL Pro League Season 20"
  team1: Team;
  team2: Team;
  status: "upcoming" | "live" | "finished";
  odds: { team1Win: number; team2Win: number; bookmaker: "BetBoom" }[];
  stats?: { maps: string[] };
}

interface Team {
  name: string;             // "FaZe Clan"
  logo: string;             // URL логотипа с HLTV
  players: Player[];
  winRate: number;          // 0-100
  recentForm: ("W" | "L")[]; // Последние 5 игр
  score: number;            // Текущий счет
}
```

### 🎨 UI индикаторы

Приложение показывает статус данных:
- 🤖 **AI-данные (реальные матчи)** - фиолетовый бейдж с Wi-Fi иконкой
- ⏳ **Загрузка...** - оранжевый бейдж когда данные загружаются

### 🛠️ Настройки

Можно настроить:
- Интервал обновления в `contexts/MatchesContext.tsx`
- Количество матчей в prompt в `services/aiWebScraper.ts`
- Список команд и турниров в prompt

---

## Важно понимать

❗ Это **не реальные live данные** с HLTV, а **AI-генерированные реалистичные матчи**

✅ Но они основаны на:
- Реальных командах CS2
- Актуальных турнирах  
- Знаниях AI о киберспорте

🎯 **Для реальных live данных** потребуется:
- Backend-сервер для обхода CORS
- Официальные API (которые стоят денег)
- WebSocket для live-обновлений
