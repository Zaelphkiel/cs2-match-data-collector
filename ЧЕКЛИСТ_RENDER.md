# ✅ Чеклист настройки Render

## Шаг 1: Команды сборки и запуска

### Build Command (команда сборки):
```bash
curl -fsSL https://bun.sh/install | bash && export PATH="$HOME/.bun/bin:$PATH" && bun install
```

### Start Command (команда запуска):
```bash
export PATH="$HOME/.bun/bin:$PATH" && bun run backend/server.ts
```

**⚠️ ВАЖНО:** Копируйте команды ПОЛНОСТЬЮ, одной строкой!

---

## Шаг 2: Environment Variables (переменные окружения)

Добавьте эти 3 переменные:

| Название | Значение |
|----------|----------|
| `PORT` | `10000` |
| `NODE_ENV` | `production` |
| `PANDASCORE_API_TOKEN` | ваш токен с https://pandascore.co/ |

### Как добавить переменные:
1. Render Dashboard → Ваш сервис
2. Environment → Add Environment Variable
3. Введите название и значение
4. Нажмите Add

---

## Шаг 3: Проверка

### ✅ Что должно быть:
- [ ] Build Command скопирована полностью
- [ ] Start Command скопирована полностью
- [ ] Все 3 переменные добавлены
- [ ] PANDASCORE_API_TOKEN не пустой
- [ ] Сервис перезапущен (Manual Deploy)

### ✅ Проверка в браузере:
Откройте: `https://ваш-url.onrender.com/api/health`

Должно показать JSON:
```json
{
  "status": "ok",
  "message": "Backend server is running"
}
```

### ✅ Проверка в приложении:
1. Settings → Проверить подключение
2. Должна появиться зелёная галочка ✅

---

## Частые ошибки

### ❌ Ошибка: "Cannot find module"
**Решение:** Не скопировали полностью Build Command. Проверьте, что там есть `curl -fsSL...`

### ❌ Ошибка: "404 Not Found"
**Решение:** 
1. Проверьте Start Command
2. Должно быть: `bun run backend/server.ts`
3. НЕ должно быть: `npm start` или `node server.js`

### ❌ Ошибка: "PANDASCORE_API_TOKEN is not defined"
**Решение:** Добавьте переменную в Environment Variables

### ❌ Сервис "засыпает"
**Решение:** Это нормально для бесплатного плана. Первый запрос займёт 20-30 секунд.

---

## Дополнительные API (опционально)

Если хотите добавить больше источников данных:

| Название | Откуда взять | Зачем |
|----------|--------------|-------|
| `BETBOOM_API_KEY` | BetBoom API | Коэффициенты букмекера |
| `HLTV_API_KEY` | HLTV | Статистика команд |

**Примечание:** Приложение работает и без них, используя PANDASCORE.

---

## URL вашего сервера

Найдите URL вашего сервера:
1. Render Dashboard → Ваш сервис
2. Вверху страницы, рядом с названием
3. Обычно вида: `https://xxx.onrender.com`

Этот URL нужно добавить в файл `env` в проекте:
```
EXPO_PUBLIC_RORK_API_BASE_URL=https://ваш-url.onrender.com
```

**⚠️ БЕЗ слеша `/` в конце!**

---

## Готово! 🎉

После настройки всё должно работать:
- ✅ Backend сервер запущен
- ✅ Приложение подключается к серверу
- ✅ Загружаются реальные матчи
- ✅ AI анализ использует live данные

Если проблемы - смотрите `QUICK_FIX_404.md`
